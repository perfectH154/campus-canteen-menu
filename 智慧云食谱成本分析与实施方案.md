# 智慧云食谱 H5 成本分析与实施方案

> 适用范围：比赛演示版，以及后续是否升级为校内正式服务的决策参考。  
> 更新日期：2026 年 9 月

## 1. 结论与推荐方案

本项目第一阶段采用 **纯前端本地交互 H5**，不建设后端服务器、数据库、正式账号或身份认证系统。

推荐组合如下：

| 环节 | 采用方案 | 费用 | 作用 |
| --- | --- | ---: | --- |
| 团队协作、代码与初始菜单数据 | GitHub 公开仓库 | 0 元 | 管理代码、菜品 JSON、图片和修改记录 |
| 面向用户的网页发布 | 腾讯云 EdgeOne Pages 免费服务 | 0 元 | 连接 GitHub 后自动发布，供手机和电脑访问 |
| 备用访问地址 | GitHub Pages | 0 元 | 保留已发布版本，便于演示前兜底 |
| 演示过程中的编辑数据 | 浏览器 LocalStorage | 0 元 | 保存当前设备上的新增、编辑和档口绑定变化 |
| 默认访问网址 | 平台提供的二级域名 | 0 元 | 不购买独立域名即可访问 |

因此，在不购买独立域名的前提下，比赛演示版的 **直接现金成本为 0 元**。主要成本为初始菜单整理、图片准备、标签核对和演示数据维护的人力。

采用该方案的原因是：GitHub 适合多人协作和版本回退；EdgeOne Pages 适合发布静态前端网页；浏览器本地存储可以支撑演示版的菜品新增、编辑和商家档口绑定，无需为比赛额外搭建后端。

## 2. 与“纯静态展示版”的区别

本项目不是只能浏览的静态页面，而是 **前端可交互演示版**。

学生端、商家端和后勤端共用同一份浏览器内的数据：

1. 商家新增或编辑本档口菜品后，学生端列表立即更新。
2. 后勤编辑任意菜品后，学生端和商家端立即更新。
3. 后勤重新绑定商家与档口后，商家端“我的档口”立即显示新的管理范围。
4. 刷新页面后，当前浏览器仍保留操作结果。
5. 点击“恢复初始演示数据”后，浏览器删除本地修改，恢复代码中预置的比赛数据。

上述变化只发生在当前浏览器和当前设备中，不会同步到其他手机、电脑或浏览器。这是比赛演示版的预期边界。

## 3. 第一期功能边界

### 3.1 已包含的功能

| 角色 | 功能 |
| --- | --- |
| 演示入口 | 学生端、商家端、后勤端三个入口；明确标注为演示数据 |
| 学生端 | 菜品名称包含匹配搜索；按口味、餐厅和价格筛选；查看菜品图片、价格、口味、餐厅、楼层和档口 |
| 商家端 | 切换演示商家；浏览全部菜品；查看本档口菜品；新增和编辑本档口菜品 |
| 后勤端 | 浏览学生端；新增和编辑任意档口菜品；修改演示商家与档口的绑定关系 |
| 数据管理 | 浏览器本地保存；恢复初始演示数据 |

### 3.2 不包含的功能

以下功能不属于当前比赛演示版，因此不需要为其支付服务器、数据库或安全维护成本：

- 真实登录、学号认证和商家实名认证；
- 跨手机或跨电脑的数据同步；
- 评论、评分、点赞、投诉和图片投稿；
- 实时售罄、库存、订单、支付和配送；
- 后台审核流、修改记录、批量导入和统计报表；
- 云端原图存储和真实生产级备份。

## 4. 技术架构与数据流

```text
GitHub 仓库
  ├─ H5 页面代码
  ├─ 初始菜品、档口、商家 JSON
  └─ 压缩后的演示菜品图片
        │
        ├─ 自动发布到 EdgeOne Pages（主要访问入口）
        └─ 自动发布到 GitHub Pages（备用访问入口）
        │
        ▼
用户浏览器
  ├─ 读取初始演示数据
  ├─ 将新增、编辑、权限绑定写入 LocalStorage
  ├─ 学生端、商家端、后勤端即时读取同一份本地数据
  └─ “恢复初始演示数据”清除本地修改
```

### 4.1 数据结构

演示数据只保留三类核心记录：

| 数据 | 必要字段 |
| --- | --- |
| 菜品 | 编号、名称、图片、基础款价格、口味列表、餐厅编号、楼层编号、档口编号 |
| 档口 | 档口编号、餐厅名称、楼层、档口名称 |
| 演示商家 | 商家编号、商家名称、被分配档口编号 |

不保存评分、评价、库存、供应数量、供应状态或用户个人信息。

### 4.2 图片处理原则

比赛演示优先使用预置的压缩图片或示意图。商家端上传的图片可压缩为浏览器本地数据后保存，但不适合储存大量高清原图。

建议单张图片控制在 300 KB 左右，优先使用 WebP 或压缩后的 JPG，并统一使用接近 4:3 的画面比例。这样可以避免网页加载慢，也减少浏览器本地存储空间不足的概率。

## 5. 第一期成本分析

### 5.1 直接现金成本

| 成本项目 | 第一期方案 | 年成本 | 说明 |
| --- | --- | ---: | --- |
| 云服务器 | 不使用 | 0 元 | 页面不运行 Node.js、Python、Java 等后端程序 |
| 数据库 | 不使用 | 0 元 | 演示数据使用 JSON 和 LocalStorage |
| 网页托管 | EdgeOne Pages 免费服务 | 0 元 | 发布静态 H5 页面 |
| 备用托管 | GitHub Pages | 0 元 | 作为备用公开网址 |
| HTTPS 证书 | 平台自动提供 | 0 元 | 不单独购买证书 |
| 初始图片存储 | 随网页静态资源发布 | 0 元 | 使用压缩后的少量演示图片 |
| 默认网址 | 平台二级域名 | 0 元 | 不购买独立域名 |
| **合计** |  | **0 元/年** | 不含人力成本 |

平台免费额度和服务规则可能调整。比赛前应使用计划采用的校园网络和手机网络打开页面，确认实际访问速度和稳定性；GitHub Pages 作为备用入口。

### 5.2 可选支出

| 项目 | 是否需要 | 预计费用 | 触发条件 |
| --- | --- | ---: | --- |
| 自定义域名 | 非必需 | 约 50–150 元/年 | 需要更正式、易记的网址时 |
| 国内对象存储与 CDN | 非必需 | 按实际用量 | 大量真实高清菜品图或访问量明显增长时 |
| 校内服务器或校内域名 | 非必需 | 由学校条件决定 | 学校要求接入校内统一平台时 |
| 后端与数据库 | 当前不需要 | 后续单独评估 | 需要跨设备同步、真实账号或线上管理时 |

如后期选择中国内地对象存储、国内服务器和自定义域名，应根据学校和云平台要求确认域名备案、接入和安全管理流程。免费二级域名用于比赛演示时，不应将正式生产部署要求直接套用到当前版本。

### 5.3 人力成本

人力是当前版本的主要投入，建议按以下范围安排：

| 工作 | 预计投入 | 交付结果 |
| --- | --- | --- |
| 初始数据清洗 | 2–3 人，约 1–2 周 | 菜名、价格、餐厅、楼层、档口统一格式 |
| 菜品标签校对 | 约 1–2 人日 | 口味标签符合展示规则，避免明显误标 |
| 图片准备 | 约 1–3 人日 | 演示图片或示意图统一比例和体积 |
| H5 页面开发与联调 | 依页面完成度安排 | 三端角色、筛选、表单、权限演示和恢复数据可用 |
| 比赛前更新与彩排 | 每次约 30–60 分钟 | 检查演示商家、初始数据和恢复按钮 |
| 真实菜单持续维护 | 每月约 2–4 小时 | 在菜单、价格或档口变动时更新初始数据 |

如果未来要做到“每天菜单都准确”，应由食堂或后勤指定数据负责人；若没有稳定的数据来源，再低的云服务费用也无法保证信息准确。

## 6. 分阶段演进与成本变化

### 阶段 A：比赛演示版（当前）

目标是证明学生能够按口味、餐厅和价格快速找菜，并演示商家、后勤如何维护菜品和档口权限。

| 项目 | 方案 |
| --- | --- |
| 架构 | 纯前端 + 浏览器本地存储 |
| 年度现金成本 | 0 元 |
| 数据同步 | 不支持，仅当前浏览器有效 |
| 内容审核 | 不需要，无用户评论与投稿 |
| 数据恢复 | 一键恢复初始演示数据 |

### 阶段 B：公开菜单展示版

当比赛结束后，如只需让师生查询正式菜单，可继续保持静态网站架构。由固定负责人维护 JSON 文件，提交后自动发布。

| 项目 | 方案 |
| --- | --- |
| 架构 | 静态前端 + GitHub 数据版本管理 |
| 年度现金成本 | 0 元；购买域名时约 50–150 元/年 |
| 数据同步 | 发布后所有用户读取统一菜单数据 |
| 内容审核 | 不需要，无用户生成内容 |
| 关键投入 | 菜单更新流程和信息准确性 |

### 阶段 C：正式互动服务版

只有在确认存在稳定访问量、明确的数据责任人和学校支持后，才建议建设真实后端。

| 新增能力 | 新增技术与成本 |
| --- | --- |
| 跨设备同步 | 后端接口、数据库、账号体系和备份 |
| 商家真实管理 | 身份认证、权限控制、操作日志和审核流程 |
| 用户收藏与评价 | 用户数据、内容审核、举报与申诉处理 |
| 用户上传菜品图 | 对象存储、CDN、图片压缩和内容安全处理 |
| 统计报表 | 数据采集、分析服务和权限管理 |

在低访问量并使用学生优惠或免费额度时，云资源现金支出可先按约 200–600 元/年进行测试性估算；该金额不包含开发人力、内容审核、投诉处理、学校系统对接和合规管理。正式覆盖全校前，应按实际访问量和学校部署要求重新报价。

## 7. 风险与控制方式

| 风险 | 第一期表现 | 控制方式 |
| --- | --- | --- |
| 不同设备数据不一致 | 一台设备的编辑不会同步到另一台 | 在入口和演示说明中标注“比赛演示数据”；需要同步时再升级后端 |
| 浏览器清理缓存 | LocalStorage 可能被用户手动清除 | 初始数据始终保存在网页中；可一键恢复 |
| 本地图片过大 | 可能占满浏览器存储、导致保存失败 | 压缩图片，限制图片尺寸和数量 |
| 菜单信息不准确 | 学生端会展示过期价格或档口 | 指定数据负责人，明确更新日期与来源 |
| 免费平台网络差异 | 不同校园网环境下速度可能不同 | 比赛前在目标网络实测；保留 GitHub Pages 备用网址 |
| 将演示版直接用于真实运营 | 无登录、无同步、无审计 | 明确演示版边界，正式上线前重新评估架构 |

## 8. 实施与发布流程

1. 团队在 GitHub 仓库维护 H5 代码、初始 JSON 和演示图片。
2. 每次更新前由负责人检查菜名、价格、餐厅、楼层、档口和口味标签。
3. 修改经团队确认后合并到 `main` 分支。
4. EdgeOne Pages 自动构建并发布最新页面；GitHub Pages 保留备用版本。
5. 比赛前在手机和电脑分别检查学生端、商家端、后勤端、表单校验和恢复初始数据按钮。
6. 比赛中若需要重复展示，点击“恢复初始演示数据”即可回到预设状态。

## 9. 最终成本结论

智慧云食谱 H5 的比赛演示版不需要后端服务器。采用 GitHub 协作、免费静态托管和浏览器本地存储后，服务器、数据库、HTTPS、自动发布和默认访问地址的直接现金成本均为 0 元。

项目当前最值得投入的部分是菜单数据准确性、菜品图片质量、口味标签规则、商家档口演示数据和比赛流程彩排。只有在项目转向真实多人使用、跨设备同步、用户评论或正式商家管理时，才需要增加后端、数据库、对象存储、身份认证和内容审核预算。
