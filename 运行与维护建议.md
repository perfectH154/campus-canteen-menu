# 运行与维护建议

## 这版原型

网页读取静态 `menu.json` 和 4 张本地菜品示意图，不写入共享服务器，也没有账号、数据库、用户上传图片存储或支付接口。离线本地运行没有云资源费用；如放到免费静态托管，主要支出是菜单维护人工。

### 免费托管选择

一期推荐保留 GitHub 公开仓库作为团队协作和代码版本库，并将网页部署到 **腾讯云 EdgeOne Pages 免费服务**。代码提交到 GitHub 后可自动发布，用户从 EdgeOne Pages 的公开网址直接访问；不购买自定义域名时，托管、HTTPS 证书、服务器和数据库均不产生费用。EdgeOne Pages 面向静态网站和前端应用，支持连接 Git 仓库自动构建和发布。[腾讯云 EdgeOne Pages 产品说明](https://edgeone.cloud.tencent.com/pages/)

| 组成 | 一期选择 | 直接费用 | 作用 |
| --- | --- | --- | --- |
| 源码、菜单数据与团队协作 | GitHub 公开仓库 | 0 元 | 管理代码、菜单 JSON 和修改记录 |
| 网站发布与用户访问 | 腾讯云 EdgeOne Pages 免费服务 | 0 元 | 自动部署网页，供电脑和手机访问 |
| 默认网站地址 | 平台提供的二级域名 | 0 元 | 试运行、内部推广和扫码访问 |
| 自定义域名 | 暂不购买 | 0 元 | 后续需要正式品牌网址时再增加 |

GitHub Pages 可继续作为备用发布地址。Cloudflare Pages 的静态资源请求也免费且不限量，适合面向国际用户或作为技术备用，但校内和中国大陆网络环境下的实际访问效果应在试用阶段测量后再决定是否启用。[Cloudflare Pages 计费说明](https://developers.cloudflare.com/pages/functions/pricing/)

当前原型收藏仅写在访问者自己的浏览器里。按键入关键词和品牌筛选不产生后端费用。

## 建议分阶段

| 阶段 | 内容 | 运行成本 | 维护重点 |
| --- | --- | --- | --- |
| 试用 | 保持静态页；修正菜品、档口和价格 | 本地运行可为 0；部署到现有校园静态空间通常不需要另建服务器 | 指定一位数据负责人；收集错价、漏项和搜索不到的问题 |
| 团队协作与发布 | GitHub 公开仓库保存源码和菜单 JSON；`main` 更新后自动发布到 EdgeOne Pages | 使用免费服务时为 0 元，不需自建服务器 | 保护主分支，团队用 PR 校对价格、时段、标签及图片；提交新 JSON 后自动更新网页 |
| 日常更新 | 商户或餐饮管理员每日批量更新，发布新 JSON | 仍可无数据库；成本主要是人工和现有托管条件 | 固定表格模板、更新时间、审核人；价格为 0 的含义要先确认 |
| 标签校正 | 复核口味和形式的规则初标，记录人工纠正 | 静态标签几乎不增加云费用；需要内容校对时间 | 保留人工确认标签作为训练样本；不要把菜名词项规则当作已训练模型 |
| 供应时段 | 按来源表的“早餐”与“午餐、晚餐”工作表归类；补齐其余档口时段 | 静态字段，无新增后端成本 | 其余 546 条暂标待确认；工作表归类不代表实际供应时段，须由档口核对 |
| 菜品照片 | 将 4 张示意图替换为档口授权、菜品对应的真实照片 | 静态图片会增加少量仓库体积和流量，无需独立图片服务 | 先统一拍摄背景、裁切比例和文件大小；缺图时继续显示示意图 |
| 正式域名与国内生产部署 | 访问量、推广范围稳定后再购买域名；如学校要求国内云资源，可选择 COS/OSS 加 CDN | 域名通常按年付费；对象存储和 CDN 按实际用量计费 | 中国内地存储桶绑定自定义域名通常涉及 ICP 备案；上线前由学校信息化部门确认 |
| 交我办接入 | 接入统一身份、商户后台、照片、跨设备收藏 | 需要联调、后台和存储；用量与学校现有采购、用户量和图片流量有关 | 权限、账号交接、备份和故障联系人 |
| 评价评论 | 点赞、评分、留言及运营看板 | 需要数据库与接口；评论需要审核、举报和处理人力 | 内容审核规则、申诉处理、异常评分监控 |

如果必须另建微信端后端，腾讯云 CloudBase 官方当前页面列出免费体验额度，以及个人版 ¥19.9/月（限时优惠）和标准版 ¥199/月；免费体验版有资源额度、功能和有效期限制，实际账单还受套餐、额外用量、存储与图片处理影响。它可作为早期预算参照，不应直接当作学校正式环境报价。[CloudBase 资源点及套餐价格](https://cloud.tencent.com/document/product/876/127357)

## 粗略维护工时

汇报稿的目标包含商户每日更新。如果由餐饮管理员每日合并并抽查一次，按每个工作日 10–20 分钟估算，约为每月 4–7 小时；这是试运行前的工作量假设，不是已承诺服务水平。若开放评论，还需另行安排审核和用户反馈响应时间。

## 开发前应补齐

1. 确认数字价格生效日期、0 元记录含义、文字标记规则，以及谁负责每日确认。
2. 向各档口补照片、营业时间、菜品下架/售罄状态；当前清单并无这些字段。
3. 先和“交我办”及学校信息化部门确认接入、账号、部署位置和审核流程，再决定是否做小程序后端。
4. 用静态原型试用一周，记录访问量、错价率和更新用时；据此估算图片存储、后端规格及日常人力。
