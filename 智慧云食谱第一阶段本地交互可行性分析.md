# 智慧云食谱第一阶段本地交互可行性分析

## 可行性分析

智慧云食谱拟分阶段推进。第一阶段聚焦比赛展示和短期验证，采用本地交互 H5 的方式完成学生端、商家端和后勤端的核心流程；后续再根据师生反馈、菜单更新频率和后勤支持程度，评估是否建设真实登录、云端同步和正式后台。第一阶段不涉及服务器采购、正式身份认证或跨设备数据同步，调整范围较小，能够在现有菜品清单的基础上快速完成，适合作为项目的先行验证版本。

第一阶段最大的难点在于：在不建设后端服务的情况下，仍需让学生查询、商家维护和后勤管理三个角色产生清晰联动。该问题的实现路径比较成熟。网页加载时读取预置的菜品、档口和演示商家数据；用户在商家端新增或编辑菜品、后勤端修改商家与档口绑定后，将变更写入当前浏览器的 LocalStorage；学生端、商家端和后勤端再从同一份本地数据中读取最新结果。因此，商家增加一道菜品后，学生端可以立即显示该菜品；后勤改变商家绑定的档口后，商家端也会立即切换到新的可管理范围。搜索、口味筛选、餐厅筛选、价格区间筛选和表单校验均属于浏览器能够直接处理的功能，开发难度和维护成本较低。

对于菜品数据准备，现有的菜品名称、价格、餐厅、楼层和档口等信息可以作为初始演示数据。口味标签采用原味、清淡、咸鲜、甜味、酸甜、酱香、蒜香、香辣、麻辣、酸辣和咖喱等固定选项；对清单中未直接提供口味的数据，可先依据菜名进行规则初标，再由团队人工复核。这样既能够支持学生端的组合筛选，也能避免在比赛阶段引入复杂的模型训练、拼音搜索或自然语言搜索。菜品图片可优先使用压缩后的示意图或代表性实拍图，在保证页面展示效果的同时，控制网页体积和浏览器本地存储占用。

本地交互版本的另一项重点是保证比赛演示可重复。页面入口将明确标注“当前网站为比赛演示版本，页面中的商家和权限均为演示数据”。商家端使用预设的演示商家选择框，后勤端可调整其档口绑定；每次演示结束后，点击“恢复初始演示数据”即可清除当前浏览器中的修改并回到预置状态。该设计可以避免多轮展示中前一次操作影响后一次结果，也使评委能够完整看到学生找菜、商家维护和后勤分配权限的全过程。

从成本和部署条件看，第一阶段具备较高可行性。项目代码和初始数据可由 GitHub 仓库统一管理，网页使用免费静态托管服务发布，浏览器本地存储代替数据库。因而服务器、数据库、HTTPS 证书、默认访问地址和基础图片存储均不产生直接现金支出。项目的主要投入是整理菜名、价格、档口和图片，核对口味标签，准备演示商家数据，并在比赛前使用手机、电脑和现场网络完成一次流程彩排。

需要说明的是，本地交互版本只适用于比赛展示和功能验证。当前设备中的编辑结果不会自动同步到其他手机、电脑或浏览器；前端展示的商家权限也不能替代真实身份认证和服务端权限校验。若项目后续需要面向真实商家和学生长期使用，再增加统一身份认证、云端数据库、图片云存储、操作记录和服务端权限控制即可，现有的学生端、商家端和后勤端页面结构仍可继续复用。

综上，智慧云食谱第一阶段本地交互方案在技术、数据、成本和展示效果方面均具有较高可行性。该方案充分利用现有菜品清单和成熟的浏览器能力，以较低的开发和维护成本完成核心流程验证，能够在短期内形成可访问、可操作、可重复演示的比赛成果，并为后续正式系统建设保留清晰的升级路径。
